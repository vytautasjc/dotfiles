set -g mouse off

# Pane splits should open to the same path as the current pane
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# Pane top border with name
set -g pane-border-status top
set -g pane-border-format " #{pane_index} #{pane_title} : #{pane_current_path} "
setw -g pane-active-border-style fg=colour153,bg=colour234
setw -g pane-border-style fg=colour243,bg=colour235

# Darken inactive pane
setw -g window-active-style fg=colour252,bg=colour234
setw -g window-style fg=colour247,bg=colour235

# Address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# Increase scrollback buffer size from 2000 to 50000 lines
set -g history-limit 50000

# Increase tmux messages display duration from 750ms to 4s
set -g display-time 4000

# Refresh 'status-left' and 'status-right' more often, from every 15s to 5s
set -g status-interval 5

# Upgrade $TERM
set -g default-terminal "screen-256color"

# Focus events enabled for terminals that support them
set -g focus-events on

# Super useful when using "grouped sessions" and multi-monitor setup
setw -g aggressive-resize on

# Easier and faster switching between next/prev window
bind C-p previous-window
bind C-n next-window

# Source tmux.conf as suggested in `man tmux`
bind-key R if-shell '[ -n "$XDG_CONFIG_HOME" ]' \
  'source-file "$XDG_CONFIG_HOME/tmux/tmux.conf"' \
  'source-file "$HOME/.config/tmux/tmux.conf"'